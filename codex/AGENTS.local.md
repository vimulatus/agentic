This machine is my Mac.

- Open an HTML file, a PDF or an image with `open <path>`. It goes to the default app.

- Wine Stable 11.0 is installed at /Applications/Wine Stable.app; wine is available at /opt/homebrew/bin/wine. XpertErp.EXE and its bundled VFP9 runtime load in an isolated prefix, but startup asks for setup; setup reports a missing MySQL folder. Test logs: /private/tmp/vimulatus/xpert-wine/.

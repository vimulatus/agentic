{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/pr-opened.sh\"",
            "timeout": 10,
            "statusMessage": "Checking for a new PR"
          }
        ]
      }
    ]
  }
}
